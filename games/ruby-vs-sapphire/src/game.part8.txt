ood);document.getElementById(`${p}-stone`).textContent=Math.floor(k.resources.stone);document.getElementById(`${p}-iron`).textContent=Math.floor(k.resources.iron);document.getElementById(`${p}-army`).textContent=k.soldiers().length;document.getElementById(`${p}-era`).textContent=`${['','SURVIVAL','AGRICULTURE','CRAFTSMANSHIP','METALLURGY','KINGDOM'][k.era]} · ${CONTENT.stages[k.stage].toUpperCase()}`;}document.getElementById('relation').textContent=relation}
function dayNight(t){const phase=(t%CONFIG.daySeconds)/CONFIG.daySeconds,day=.30+.70*Math.max(0,Math.sin(phase*Math.PI*2));hemi.intensity=.30+.58*day;sun.intensity=.20+.70*day}
function overview(){camera.setTarget(v3(0,2,0));camera.radius=125;camera.alpha=-Math.PI/2;camera.beta=.92}

function inspect(meta){if(!meta)return;let title='',html='';if(meta.kind==='npc'){const n=meta.ref;title=n.name;html=`Kingdom: <b>${n.kingdom.name}</b><br>Role: <b>${n.role}</b><br>Health: ${Math.max(0,n.health|0)}/${n.maxHealth}<br>Hunger: ${Math.max(0,n.hunger|0)}%<br>Task: ${n.task}<br>Carry: ${n.carry||'—'}`;}else if(meta.kind==='building'){const b=meta.ref;title=`${b.kingdom.name} ${b.definition.label}`;html=`Variant: ${b.variant+1}<br>Health: <b>${Math.max(0,b.hp|0)}/${b.maxHp}</b><br>Blocks remaining: ${b.blocks.length}<br>Status: ${b.destroyed?'Destroyed':'Operational'}`;}else if(meta.kind==='buildingProject'){const p=meta.ref,done=p.blocks.filter(x=>x.placed).length;title=`${p.kingdom.name} ${p.definition.label}`;html=`Construction: <b>${Math.floor(done/p.blocks.length*100)}%</b><br>Blocks: ${done}/${p.blocks.length}<br>Builders physically fetch material before every placement.`;}else if(meta.kind==='siege'){title=`${meta.ref.kingdom.name} Catapult`;html=`Health: ${meta.ref.health}<br>Role: siege artillery<br>Range: 24<br>Projectile: stone boulder`;}else if(meta.kind==='resource'){title=`${meta.type.toUpperCase()} RESOURCE`;html='Natural resource node';}else return;document.getElementById('inspect-title').textContent=title;document.getElementById('inspect-body').innerHTML=html;document.getElementById('inspect').classList.remove('hidden')}
scene.onPointerObservable.add(pi=>{if(pi.type===BABYLON.PointerEventTypes.POINTERPICK){const m=pi.pickInfo?.pickedMesh;if(m?.metadata)inspect(m.metadata)}});

function chooseTeam(user,k){const key=String(user).toLowerCase();if(viewerTeams.has(key))return;viewerTeams.set(key,k.id);k.supporters++;addEvent(`${user} joined Team ${k.name}!`,k.id)}
function viewerKingdom(user){const id=viewerTeams.get(String(user).toLowerCase());return id==='ruby'?ruby:id==='sapphire'?sapphire:null}
function liveEvent(e){const type=e.type.toLowerCase();if(type==='chat'){const msg=e.comment.trim().toLowerCase();if(/\bruby\b/.test(msg))chooseTeam(e.user,ruby);else if(/\bsapphire\b/.test(msg))chooseTeam(e.user,sapphire)}else if(type==='like'){const k=viewerKingdom(e.user);if(k)k.resources.food+=Math.min(10,e.likes*.08)}else if(type==='follow'){let k=viewerKingdom(e.user);if(!k)k=Math.random()<.5?ruby:sapphire;if(k.population()<CONFIG.popCap){spawnNPC(k,'gatherer',e.user);k.resources.food+=4;addEvent(`${e.user} followed: a new settler joined ${k.name}.`,k.id)}}else if(type==='share'){const k=viewerKingdom(e.user);if(k){k.resources.wood+=3;k.resources.food+=3}}else if(type==='gift'){let k=viewerKingdom(e.user);if(!k)k=Math.random()<.5?ruby:sapphire;const d=Math.max(1,e.diamonds||1),boost=Math.min(160,Math.max(3,Math.sqrt(d)*3.5));k.resources.food+=boost*.34;k.resources.wood+=boost*.30;k.resources.stone+=boost*.24;if(d>=50)k.resources.iron+=Math.max(1,Math.floor(Math.sqrt(d)/4));if(d>=500&&relation==='WAR')spawnCatapult(k);addEvent(`${e.user} sent ${e.giftName} (${d}💎) → ${k.name}.`,k.id)}}

const bridge=new TikFinityBridge({onEvent:liveEvent,onStatus:(s)=>{const dot=document.getElementById('bridge-dot'),label=document.getElementById('bridge-label');dot.className=s==='live'?'live':(s==='error'?'error':'');label.textContent=s==='live'?'TIKFINITY LIVE':s==='connecting'?'CONNECTING…':s==='offline'?'RECONNECTING…':'TEST MODE';}});
document.getElementById('toggle-live').onclick=()=>bridge.connect();document.getElementById('toggle-test').onclick=()=>{bridge.disconnect();document.getElementById('test-panel').classList.toggle('hidden')};
document.querySelectorAll('[data-test]').forEach(b=>b.onclick=()=>{const [kind,team]=b.dataset.test.split('-'),user=`Test_${team}`;if(kind==='join')bridge.inject('chat',{user,comment:team});if(kind==='like')bridge.inject('like',{user,likes:50});if(kind==='follow')bridge.inject('follow',{user});if(kind==='gift')bridge.inject('gift',{user,giftName:'Test Gift',diamonds:100})});
document.querySelectorAll('[data-speed]').forEach(b=>b.onclick=()=>{speed=Number(b.dataset.speed);document.querySelectorAll('[data-speed]').forEach(x=>x.classList.toggle('active',x===b))});document.getElementById('overview').onclick=overview;document.getElementById('restart').onclick=()=>location.reload();document.getElementById('close-inspect').onclick=()=>document.getElementById('inspect').classList.add('hidden');

function sim(dt){if(worldEnded)return;simTime+=dt;replenishResources(dt);for(const k of kingdoms){economy(k,dt);technology(k,dt);populationGrowth(k,dt);rebalanceBuilders(k);planKingdom(k,dt);recruit(k,dt);siegeProduction(k,dt)}for(const n of npcs)n.update(dt);for(const s of siegeEngines)s.update(dt);diplomacy(dt);checkVictory();updateHUD()}

makeWorld();createCamp(ruby);createCamp(sapphire);overview();updateHUD();
engine.runRenderLoop(()=>{const dt=Math.min(.05,engine.getDeltaTime()/1000);dayNight(simTime);if(speed>0){accumulator+=dt*speed;let guard=0;while(accumulator>=CONFIG.simTick&&guard++<16){sim(CONFIG.simTick);accumulator-=CONFIG.simTick}updateProjectiles(dt*speed)}scene.render()});window.addEventListener('resize',()=>engine.resize());
})();
